import React from "react";

const Home = () => {
  const style = {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "100vh",
    fontSize: "3rem", // Adjust the font size as needed
    fontWeight: "bold",
  };

  return (
    <div style={style}>
      <h1>Akeed loves Faiza!</h1>
    </div>
  );
};

export default Home;
